# نصب دفتر کلمه روی cPanel

این بسته برای هاست اشتراکی دارای PHP 8.1 یا بالاتر و MySQL/MariaDB آماده شده است و به Node.js، Composer یا Cloudflare نیاز ندارد.

## پیش‌نیازها

- PHP 8.1 یا بالاتر
- افزونه‌های `pdo_mysql`، `fileinfo`، `json` و `session`
- Apache با `mod_rewrite`
- گواهی SSL فعال
- حداقل 128 مگابایت حافظه PHP
- `upload_max_filesize` و `post_max_size` حداقل 21 مگابایت

## نصب

1. از cPanel وارد **MySQL Databases** شوید و یک دیتابیس و یک کاربر بسازید.
2. کاربر را با سطح دسترسی **ALL PRIVILEGES** به دیتابیس متصل کنید.
3. فایل‌های داخل بسته `kalameh-office-cpanel` را در Document Root دامنه یا زیردامنه آپلود و Extract کنید. خود پوشهٔ والد را آپلود نکنید؛ `index.html` باید مستقیماً داخل Document Root باشد.
4. آدرس `https://YOUR-DOMAIN/install.php` را باز کنید.
5. اطلاعات MySQL و حساب مدیر کل را در فرم وارد کنید و نصب را بزنید.
6. وارد سامانه شوید و عملکرد ورود، ذخیره اطلاعات و آپلود فایل را بررسی کنید.
7. پس از نصب موفق، فایل `install.php` را از File Manager حذف کنید.

## ساخت حساب کارمندان

مدیر کل از بخش «اعضای تیم» نام، ایمیل، رمز و سطح دسترسی هر کارمند را تعیین می‌کند. کارمند با همان ایمیل و رمز از صفحه ورود وارد می‌شود.

## نگهداری و نسخه پشتیبان

- از دیتابیس با phpMyAdmin خروجی SQL بگیرید.
- از پوشه `storage/uploads` و فایل `storage/config.php` نسخه پشتیبان رمزگذاری‌شده نگه دارید.
- پوشه `storage` به‌وسیله `.htaccess` از دسترسی مستقیم وب محافظت می‌شود.
- اطلاعات دیتابیس یا رمز مدیر را در GitHub قرار ندهید.
